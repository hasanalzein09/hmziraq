# دليل النشر - hmziraq

## ⚠️ مشكلة البناء المحلي

المشروع يحتوي على **278 خدمة تقنية** في ملف واحد (`services.ts` = 142KB)، مما يسبب **نفاذ ذاكرة Node.js** أثناء البناء المحلي حتى مع 16GB!

```bash
FATAL ERROR: JavaScript heap out of memory
```

## ✅ الحل الأمثل: النشر المباشر على Vercel

Vercel عندها **build servers** أقوى بكثير من الأجهزة المحلية وتدعم Next.js بشكل مثالي.

### خطوات النشر على Vercel:

1. **إنشاء حساب Vercel مجاني**
   ```bash
   # تسجيل دخول من Terminal
   npx vercel login
   ```

2. **ربط المشروع بـ GitHub**
   ```bash
   git init
   git add .
   git commit -m "Initial commit - 278 technical services"
   git remote add origin YOUR_GITHUB_REPO_URL
   git push -u origin main
   ```

3. **رفع على Vercel**
   ```bash
   # طريقة 1: من Terminal
   npx vercel --prod

   # طريقة 2: من Dashboard
   # اذهب إلى vercel.com/new
   # اربط GitHub repo
   # اضغط Deploy
   ```

4. **إعدادات Build في Vercel Dashboard:**
   - **Build Command:** `next build`
   - **Output Directory:** `.next`
   - **Install Command:** `npm install`
   - **Node.js Version:** `20.x` (أحدث)

### مميزات النشر على Vercel:

✅ **Build بدون مشاكل ذاكرة** - خوادم قوية جداً  
✅ **CDN عالمي** - سرعة تحميل ممتازة في العراق  
✅ **SSL مجاني** - HTTPS تلقائي  
✅ **Auto-deployment** - كل push على GitHub ينشر تلقائياً  
✅ **ISR Support** - الـ 77,840 صفحة تتولد On-Demand  
✅ **مجاني للمشاريع الشخصية**

---

## 🔧 البديل: التطوير المحلي (Dev Mode)

إذا تريد تشغيل المشروع محلياً للتطوير:

```bash
npm run dev
```

هذا **يشتغل بدون مشاكل** لأنه ما يبني كل الصفحات مرة وحدة.

---

## 📊 معلومات المشروع

- **عدد الخدمات:** 278 خدمة تقنية
- **عدد أنواع الأعمال:** 40 نوع
- **عدد المدن:** 7 مدن عراقية
- **إجمالي الصفحات المحتملة:** 278 × 40 × 7 × 2 لغات = **155,680 صفحة**
- **الصفحات المبنية مسبقاً:** 100 صفحة استراتيجية فقط
- **الباقي:** يتولد On-Demand عند أول زيارة (ISR)

---

## 🚀 الخطوات التالية

1. ارفع المشروع على GitHub ✅
2. اربطه بـ Vercel ✅
3. انتظر Build (5-10 دقائق أول مرة) ⏳
4. اختبر الموقع الحي 🌐
5. اربط دومين خاص (إذا موجود) 🔗

---

## 💡 ملاحظات

- **لا تحاول `npm run build` محلياً** - سيفشل دائماً بسبب حجم services.ts
- **استخدم `npm run dev` للتطوير** - يشتغل 100%
- **Vercel هو الحل الوحيد** الذي يبني المشروع كامل بدون مشاكل

---

تم إنشاء هذا الملف بعد 17 محاولة فاشلة لبناء المشروع محلياً 😅
